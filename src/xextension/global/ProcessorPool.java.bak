/**
 * 
 */
package xextension;

/**
 * @author QiaoMingkui
 *
 */
public class ProcessorPool {
	private Processor[] pool;

	public ProcessorPool(int maxNum) {
		pool = new Processor[maxNum];
	}

	public synchronized Processor getProcessor() {
		Processor processor = null;
//		for (int i = 0; i < pool.length; i++) {
//			processor = pool[i];
//			if (processor == null) {
//				processor = new Processor();
//			}
//		}
		return processor;
	}
}
